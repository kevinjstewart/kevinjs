


	
	<div class="row">
		<div class="col-md-12">
			{{outlet}}
		</div>
	</div>

	<div class="row">

		
			{{#each}}
				<div class="col-md-4 col-sm-4">
					<div class="thumbnail">
					    <img {{bind-attr src=img}} class="img-responsive">
					    <div class="caption">
						    <h3>{{title}}</h3>
						    <p>{{tagline}}</p>
						    {{#link-to 'project' this classNames="btn btn-primary"}}
								View
						    {{/link-to}}
 
						</div>
				    </div>
				</div>
			{{/each}}


	</div>
	